{{ ansible_managed }}

[Unit]
Description=query service
After=network.target
StartLimitIntervalSec=0
[Service]
Type=simple
Restart=always
RestartSec=1
User=root
ExecStart={{ symlink }}/bin/supervise -c {{ symlink }}/conf/supervise/query.conf

[Install]
WantedBy=multi-user.target
