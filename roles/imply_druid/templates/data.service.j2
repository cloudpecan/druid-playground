{{ ansible_managed }}

[Unit]
Description=data service
After=network.target
StartLimitIntervalSec=0
[Service]
Type=simple
Restart=always
RestartSec=1
User=root
ExecStart={{ symlink }}/bin/supervise -c {{ symlink }}/conf/supervise/data.conf

[Install]
WantedBy=multi-user.target

